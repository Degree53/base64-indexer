{
  "name": "base64-indexer",
  "version": "1.0.0",
  "description": "Converts a specified set of files to base64, outputting a JSON file that contains the result of each file.",
  "main": "main.js",
  "dependencies": {
    "node-mime": "^1.0.0",
    "moment": "^2.9.0",
    "mime": "^1.3.4",
    "imagemin": "^3.1.0",
    "yargs": "^3.17.1"
  },
  "scripts": {
    "test": "mocha test/"
  },
  "author": "Karl Purkhardt",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^2.2.5",
    "must": "^0.12.0"
  }
}
